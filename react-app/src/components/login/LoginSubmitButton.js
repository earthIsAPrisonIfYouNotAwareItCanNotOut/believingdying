import React, { Component } from 'react'
import PropTypes from 'prop-types'
import Button from 'material-ui/Button'

const LoginSubmitButton = () => (
  <Button style={{ width: 300}}>submit</Button>
)


export default LoginSubmitButton
